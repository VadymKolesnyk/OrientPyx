namespace OrientPyx.Presentation.Services;

/// <summary>Shared constants for the competition-archive file format.</summary>
public static class EventArchiveConstants
{
    /// <summary>The archive file extension (a zip under the hood), without the leading dot.</summary>
    public const string Extension = "opyx";
}
